# PAN-Crafter 재현/경량화 — 실제로 import 되는 것만 추렸다.
# requirements.yaml 은 conda env 전체 export(155행)라 ffmpeg·intel-openmp 등
# 무관한 것이 섞여 있다. 새 서버·클라우드에는 이 파일을 쓰는 편이 빠르다.
#
#   pip install -r requirements.txt
#
# torch/torchvision 은 PyTorch 베이스 이미지에 이미 있으면 빼도 된다.
# CUDA 11.8 빌드 기준이며, 다른 CUDA 를 쓰면 두 줄만 바꾼다.

torch==2.4.0
torchvision==0.19.0

accelerate==0.33.0
diffusers==0.32.2
safetensors==0.4.4
timm==1.0.9
einops==0.8.0

numpy==1.26.4
scipy==1.13.1
h5py==3.12.1
scikit-image==0.24.0
opencv-python-headless==4.10.0.84

# 결과를 Google Sheets 로 올리는 데 쓴다 (gspread/gspread_upload.py)
gspread==6.2.1
gspread-formatting==1.2.1

matplotlib==3.9.4
tqdm==4.66.5
PyYAML==6.0.1
thop==0.1.1-2209072238
